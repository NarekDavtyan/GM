package com.globalgis.grand_marketing.models;

import java.util.Calendar;

public class Street extends LocalityUnit {

	public Street(int parentId, String name, Calendar last_update, int version) {
		super(parentId, name, last_update, version);
	}

}
