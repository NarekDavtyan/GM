package com.globalgis.grand_marketing.models;

import java.util.Calendar;

public class OpenedType extends Type {

	public OpenedType(String typename, Calendar last_update, int version) {
		super(typename, last_update, version);
	}

}
