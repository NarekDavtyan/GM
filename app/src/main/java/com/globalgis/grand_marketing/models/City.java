package com.globalgis.grand_marketing.models;

import java.util.Calendar;

public class City extends LocalityUnit {

	public City(int parentId, String name, Calendar last_update, int version) {
		super(parentId, name, last_update, version);
	}

}
